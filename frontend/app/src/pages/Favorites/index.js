import React from 'react';
import { Text } from 'react-native';
import { Container, LoadingIcon } from './styles';

export default () => {
    return (
      <Container>
        <Text>Favorites</Text>
      </Container>
    );
}